// JavaScript Random

// Math.random(): returns a random number between 0 and 1
console.log(Math.random());

// Random Integers
// Math.random() used with Math.flor() can be used to return random integers
let i = Math.random();
console.log(i);
console.log(Math.floor(i * 10));